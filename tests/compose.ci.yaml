services:
  configurator:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}

  backend:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}

  frontend:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}

  websocket:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}

  queue-short:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}

  queue-long:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}

  scheduler:
    image: localhost:5000/saashq/erpnexus:${ERPNEXUS_VERSION}
